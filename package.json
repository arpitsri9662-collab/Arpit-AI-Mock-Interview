{
  "name": "prepverse",
  "version": "1.0.0",
  "description": "PrepVerse - AI Interview Preparation Platform",
  "scripts": {
    "install:backend": "cd backend && npm install --include=dev",
    "install:frontend": "cd frontend && npm install --include=dev",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "install:all": "npm run install:backend && npm run install:frontend",
    "build": "npm run install:all && npm run build:backend && npm run build:frontend",
    "start": "node backend/dist/index.js",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\""
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
